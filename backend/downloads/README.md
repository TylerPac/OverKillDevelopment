Place product files here for secure delivery after purchase.

File naming convention:
- starter-pack.zip
- pro-pack.zip
- studio-pack.zip

The secured API endpoint `/shop/download/{productId}` resolves files as `${APP_SHOP_DOWNLOAD_ROOT}/{productId}.zip`.
